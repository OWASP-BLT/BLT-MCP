[project]
name = "python-mcp"
version = "0.1.0"
description = "Python MCP server for OWASP BLT integration"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.26.0",
]

